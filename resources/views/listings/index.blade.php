<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><div><h2 class="text-2xl font-black text-cyan-200">出品準備</h2><p class="mt-1 text-sm font-bold text-cyan-100">販売先ごとのタイトル、説明、価格を管理します。</p></div><a href="{{ route('listings.create') }}" class="rounded-lg bg-cyan-400 px-4 py-2 font-black text-slate-950">出品準備を作成</a></div></x-slot><div class="min-h-screen bg-slate-100 py-8"><div class="mx-auto max-w-7xl px-4"><div class="overflow-hidden rounded-lg bg-white shadow"><table class="w-full text-left text-sm text-slate-800"><thead class="bg-slate-100"><tr><th class="p-3">商品</th><th class="p-3">販売先</th><th class="p-3">価格</th><th class="p-3">見込み利益</th><th class="p-3">状態</th><th class="p-3"></th></tr></thead><tbody>@forelse($listings as $listing)<tr class="border-t"><td class="p-3 font-bold">{{ $listing->product->product_name }}</td><td class="p-3">{{ $listing->marketplace_other_name ?: $listing->marketplace->name }}</td><td class="p-3">{{ $listing->listing_price === null ? '-' : '¥'.number_format($listing->listing_price) }}</td><td class="p-3">{{ $listing->expected_profit === null ? '-' : '¥'.number_format($listing->expected_profit) }}</td><td class="p-3">{{ \App\Models\Listing::statusLabel($listing->status) }}</td><td class="p-3"><a class="font-bold text-cyan-700" href="{{ route('listings.edit', $listing) }}">編集</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">出品準備データはありません。</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $listings->links() }}</div></div></div></x-app-layout>
