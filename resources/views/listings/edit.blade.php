<x-app-layout><x-slot name="header"><h2 class="text-2xl font-black text-cyan-200">出品準備を編集</h2></x-slot><div class="min-h-screen bg-slate-100 py-8"><div class="mx-auto max-w-3xl px-4">@if(session('success'))<p class="mb-4 rounded bg-emerald-50 p-3 font-bold text-emerald-800">{{ session('success') }}</p>@endif @if(session('error'))<p class="mb-4 rounded bg-red-50 p-3 font-bold text-red-800">{{ session('error') }}</p>@endif<form method="POST" action="{{ route('listings.update', $listing) }}" class="rounded-lg bg-white p-6 shadow">@csrf @method('PUT') @include('listings.partials.form')<button class="mt-6 rounded bg-cyan-600 px-5 py-3 font-black text-white">更新する</button></form>@if($listing->status === 'draft' && $listing->sales->isEmpty())<form method="POST" action="{{ route('listings.destroy', $listing) }}" class="mt-6 border-t border-red-200 pt-6">@csrf @method('DELETE')<label class="flex items-start gap-2 text-sm font-bold text-red-900"><input name="confirm_deletion" value="1" type="checkbox" required class="mt-1">この出品下書きを削除することを確認しました。</label><button class="mt-4 rounded bg-red-700 px-4 py-2 font-bold text-white hover:bg-red-800">下書きを削除</button></form>@endif</div></div></x-app-layout>
