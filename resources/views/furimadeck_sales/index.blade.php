<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-2xl font-black text-cyan-200">取引管理</h2><a href="{{ route('furimadeck-sales.create') }}" class="rounded bg-cyan-400 px-4 py-2 font-black text-slate-950">販売を記録</a></div></x-slot>
    <div class="min-h-screen bg-slate-100 py-8"><div class="mx-auto max-w-7xl px-4">
        @if(session('success'))<p class="mb-4 rounded bg-emerald-50 p-3 font-bold text-emerald-800">{{ session('success') }}</p>@endif
        @if($errors->any())<p class="mb-4 rounded bg-red-50 p-3 font-bold text-red-800">{{ $errors->first() }}</p>@endif
        <div class="overflow-hidden rounded bg-white shadow"><table class="w-full text-left text-sm text-slate-800"><thead class="bg-slate-100"><tr><th class="p-3">商品</th><th class="p-3">販売先</th><th class="p-3">売上</th><th class="p-3">利益</th><th class="p-3">状態</th><th class="p-3">操作</th></tr></thead><tbody>@forelse($sales as $sale)<tr class="border-t"><td class="p-3 font-bold">{{ $sale->product_name_snapshot }}</td><td class="p-3">{{ $sale->marketplace->name }}</td><td class="p-3">¥{{ number_format($sale->sold_price) }}</td><td class="p-3">¥{{ number_format($sale->net_profit) }}</td><td class="p-3">{{ \App\Models\Sale::statusLabel($sale->status) }}</td><td class="p-3">@if(!in_array($sale->status, ['cancelled', 'returned'], true))<div class="space-y-2">@if($sale->status === 'awaiting_shipment')<form method="POST" action="{{ route('furimadeck-sales.status', $sale) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="shipped"><button class="rounded bg-sky-600 px-3 py-1 font-bold text-white">発送済みにする</button></form>@elseif($sale->status === 'shipped')<form method="POST" action="{{ route('furimadeck-sales.status', $sale) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="completed"><button class="rounded bg-emerald-600 px-3 py-1 font-bold text-white">取引を完了する</button></form>@endif<form method="POST" action="{{ route('furimadeck-sales.cancel', $sale) }}" class="flex flex-wrap gap-2">@csrf<input required name="reason" maxlength="1000" placeholder="キャンセル理由" class="w-32 rounded border-slate-300 text-slate-900"><button class="rounded bg-red-600 px-3 py-1 font-bold text-white">取消</button></form><details class="rounded border border-amber-300 bg-amber-50 p-2"><summary class="cursor-pointer font-bold text-amber-900">返品・返金を記録</summary><form method="POST" action="{{ route('furimadeck-sales.return', $sale) }}" class="mt-2 grid gap-2">@csrf<input required name="reason" maxlength="1000" placeholder="返品理由" class="rounded border-slate-300 text-slate-900"><label class="text-xs font-bold text-slate-700">返金額<input required min="0" type="number" name="refund_amount" value="{{ $sale->sold_price }}" class="mt-1 w-full rounded border-slate-300 text-slate-900"></label><label class="text-xs font-bold text-slate-700">返送送料<input min="0" type="number" name="return_shipping_fee" value="0" class="mt-1 w-full rounded border-slate-300 text-slate-900"></label><label class="flex items-center gap-2 text-xs font-bold text-slate-700"><input type="checkbox" name="restock" value="1">在庫へ戻す</label><button class="rounded bg-amber-500 px-3 py-1 font-bold text-slate-950" onclick="return confirm('返品・返金を記録しますか？')">返品を記録</button></form></details></div>@else@if($sale->status === 'returned')<span class="font-bold text-amber-700">返金 ¥{{ number_format($sale->refund_amount) }}</span>@else<span class="font-bold text-slate-500">取消済み</span>@endif@endif</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">販売記録はありません。</td></tr>@endforelse</tbody></table></div>{{ $sales->links() }}
    </div></div>
</x-app-layout>
